# 图像质量提升与目标识别对比

## 代码结构

```
SAR_Experiment_Project/
│
├── data/                       # 存放数据
│   ├── ATR_low_quality/        # 原始降质图像
│   └── ATR_enhanced/           # 提质后保存的图像
|   |——SAR_Cls_Dataset/         # 对于实验二的数据集放置路径
|   |    |——train/               # 实验二的训练集
|   |    |——val_enhanced/        # 把提质后的图像作为验证集
|   |    |——val_original/        # 把原始降质图像作为验证集
|   |——weights/                 # 存放实验二训练保存的最佳模型权重（根绝原始图像的准确率）
│
├── src/                        # 核心源代码目录 (模块化)
│   ├── __init__.py
│   ├── enhancement/            # 提质算法模块
│   │   ├── __init__.py
│   │   ├── filters.py          # 存放传统滤波类 ( LeeFilter)
│   │   └── enhancer.py         # 存放对比度增强类
│   ├── metrics/                # 评估指标模块
│   │   ├── __init__.py
│   │   └── evaluator.py        # 存放计算 ENL, EPI 的代码
│   ├── recognition/            # 目标识别模块
|   |   |——models/              # 存放实验二的模型类代码
|   |       |——sar_vit.py       # 改进的ViT类模型
|   |       |——sar_yolo.py      # 改进的yolo类模型    
│   └── utils/                  # 工具类
│       ├── __init__.py
│       └── data_loader.py      # 负责读取和保存图像
|       |—— data_selector.py    # 从ATRNet_STAR数据集中挑选出一部分图片进行实验二的训练数据集
|—— train/                      # 从ATRNet_STAR下载的原始数据集
│
├── main_enhance.py             # 实验一主入口 (质量提升)
└── train.py                    # 实验二主入口 (目标识别)
|—— eval_matrix.py              # 使用最佳权重对提质前后的图片进行混淆矩阵分析
|—— requirements.txt            # 运行依赖

```



## 开始实验

### 实验一

相干斑抑制、对比度增强，放弃传统的旁瓣抑制和分辨率增强
Lee滤波、CLAHE对比度增强


#### 客观评估指标
1. 等效视数，ENL: 越大越好，测背景去噪多干净
2. 边缘保持指数，EPI: 越接近于1越好，测目标轮廓是否被糊掉
3. 信杂比，SCR：越高越好
4. 信息熵： 越低越好，测图像整体信息纯度变化


### 实验二

#### 任务流程

**数据集的清洗**

Mix_trunker->Mixer_trunk
其余就字符串归一化可以解决了

在训练模型的时候，不想再单独划分验证集了，直接把原图与提升质量的图作为验证集，这样每轮训练完后，都可以在相同的损失函数下进行对比两种数据集（原图与提升质量）的结果。

585张测试图片，1170张训练图片

**模型构建**
yolov11改+ViT改

```
channelAttention：
输入Feature Map

[B,C,H,W]

        |
        |
  ----------------
  |              |
AvgPool        MaxPool

[B,C,1,1]     [B,C,1,1]

  |              |

 MLP            MLP

  |              |

[B,C,1,1]     [B,C,1,1]

       \        /

        相加

          |

       Sigmoid

          |

 Channel Attention

[B,C,1,1]

```

```
spatialAttention：
输入 Feature Map

[B,C,H,W]

        |
        |
 --------------------
 |                  |
Mean Pool        Max Pool

[B,1,H,W]       [B,1,H,W]

 |                  |

 --------Concat------

        |

[B,2,H,W]

        |

7×7 Conv

        |

[B,1,H,W]

        |

Sigmoid

        |

Spatial Attention Map

[B,1,H,W]


```
**类似Linux下的重定向到输出文件**

1.python -u train.py *> train.log  （把输出重定向到train.log文件，但是关闭powershell进程会直接中止train.py的运行,-u参数是直接输出到文件，而不是保存到内存中） 

2.

```

# ========== 第1步：后台启动 ==========
cmd /c "start /b python -u train.py > train.log 2>&1"

# ========== 第2步：记录 PID ==========
wmic process where "name='python.exe'" get ProcessId,CommandLine
# 找到 train.py 对应的 PID，记下来，比如 12345

# ========== 第3步：实时查看日志 ==========
Get-Content train.log -Wait
# 按 Ctrl+C 退出查看，不影响训练进程

# ========== 第4步：训练结束后关闭进程 ==========
Stop-Process -Id 12345 -Force

```


